from enum import Enum, auto


class BuiltinType(Enum):
    INT = auto()
    FLOAT = auto()
    STRING = auto()
    TEXT = auto()
    VECTOR = auto()
    BOOLEAN = auto()
    DATE = auto()
    JSON = auto()
